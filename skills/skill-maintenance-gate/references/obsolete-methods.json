{
  "version": 1,
  "obsolete_method_patterns": [
    "compatibility fallback that removes option-label text",
    "从.*容器.*(?:减去|删除|剔除).*(?:是/否|是、否).*(?:猜|推断|识别).*(?:字段|标签)",
    "enumerate every visible editable row control.*stable form `prop`",
    "Apply the proactive rerender scan only to real Playwright locator scopes"
  ]
}
